package chapter03;

public class StaticMethodTest {

	public static void main(String[] args) {
		// static메소드는 바로 사용이 가능하기 때문에 객체를 생성할 필요가 없다.
		int a = Math.abs(-1);
		int b = Math.max(10, 20);
		

	}

}
